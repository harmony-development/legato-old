// Code generated by entc, DO NOT EDIT.

package entgen

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/harmony-development/legato/server/db/ent/entgen/actionbutton"
)

// ActionButtonCreate is the builder for creating a ActionButton entity.
type ActionButtonCreate struct {
	config
	mutation *ActionButtonMutation
	hooks    []Hook
}

// SetText sets the "text" field.
func (abc *ActionButtonCreate) SetText(s string) *ActionButtonCreate {
	abc.mutation.SetText(s)
	return abc
}

// SetURL sets the "url" field.
func (abc *ActionButtonCreate) SetURL(s string) *ActionButtonCreate {
	abc.mutation.SetURL(s)
	return abc
}

// Mutation returns the ActionButtonMutation object of the builder.
func (abc *ActionButtonCreate) Mutation() *ActionButtonMutation {
	return abc.mutation
}

// Save creates the ActionButton in the database.
func (abc *ActionButtonCreate) Save(ctx context.Context) (*ActionButton, error) {
	var (
		err  error
		node *ActionButton
	)
	if len(abc.hooks) == 0 {
		if err = abc.check(); err != nil {
			return nil, err
		}
		node, err = abc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ActionButtonMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = abc.check(); err != nil {
				return nil, err
			}
			abc.mutation = mutation
			node, err = abc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(abc.hooks) - 1; i >= 0; i-- {
			mut = abc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, abc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (abc *ActionButtonCreate) SaveX(ctx context.Context) *ActionButton {
	v, err := abc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (abc *ActionButtonCreate) check() error {
	if _, ok := abc.mutation.Text(); !ok {
		return &ValidationError{Name: "text", err: errors.New("entgen: missing required field \"text\"")}
	}
	if _, ok := abc.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New("entgen: missing required field \"url\"")}
	}
	return nil
}

func (abc *ActionButtonCreate) sqlSave(ctx context.Context) (*ActionButton, error) {
	_node, _spec := abc.createSpec()
	if err := sqlgraph.CreateNode(ctx, abc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (abc *ActionButtonCreate) createSpec() (*ActionButton, *sqlgraph.CreateSpec) {
	var (
		_node = &ActionButton{config: abc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: actionbutton.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: actionbutton.FieldID,
			},
		}
	)
	if value, ok := abc.mutation.Text(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: actionbutton.FieldText,
		})
		_node.Text = value
	}
	if value, ok := abc.mutation.URL(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: actionbutton.FieldURL,
		})
		_node.URL = value
	}
	return _node, _spec
}

// ActionButtonCreateBulk is the builder for creating many ActionButton entities in bulk.
type ActionButtonCreateBulk struct {
	config
	builders []*ActionButtonCreate
}

// Save creates the ActionButton entities in the database.
func (abcb *ActionButtonCreateBulk) Save(ctx context.Context) ([]*ActionButton, error) {
	specs := make([]*sqlgraph.CreateSpec, len(abcb.builders))
	nodes := make([]*ActionButton, len(abcb.builders))
	mutators := make([]Mutator, len(abcb.builders))
	for i := range abcb.builders {
		func(i int, root context.Context) {
			builder := abcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ActionButtonMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, abcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, abcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, abcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (abcb *ActionButtonCreateBulk) SaveX(ctx context.Context) []*ActionButton {
	v, err := abcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
