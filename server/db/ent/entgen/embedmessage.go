// Code generated by entc, DO NOT EDIT.

package entgen

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/harmony-development/legato/server/db/ent/entgen/embedmessage"
)

// EmbedMessage is the model entity for the EmbedMessage schema.
type EmbedMessage struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EmbedMessage) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case embedmessage.FieldID:
			values[i] = &sql.NullInt64{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type EmbedMessage", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EmbedMessage fields.
func (em *EmbedMessage) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case embedmessage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			em.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this EmbedMessage.
// Note that you need to call EmbedMessage.Unwrap() before calling this method if this EmbedMessage
// was returned from a transaction, and the transaction was committed or rolled back.
func (em *EmbedMessage) Update() *EmbedMessageUpdateOne {
	return (&EmbedMessageClient{config: em.config}).UpdateOne(em)
}

// Unwrap unwraps the EmbedMessage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (em *EmbedMessage) Unwrap() *EmbedMessage {
	tx, ok := em.config.driver.(*txDriver)
	if !ok {
		panic("entgen: EmbedMessage is not a transactional entity")
	}
	em.config.driver = tx.drv
	return em
}

// String implements the fmt.Stringer.
func (em *EmbedMessage) String() string {
	var builder strings.Builder
	builder.WriteString("EmbedMessage(")
	builder.WriteString(fmt.Sprintf("id=%v", em.ID))
	builder.WriteByte(')')
	return builder.String()
}

// EmbedMessages is a parsable slice of EmbedMessage.
type EmbedMessages []*EmbedMessage

func (em EmbedMessages) config(cfg config) {
	for _i := range em {
		em[_i].config = cfg
	}
}
