// Code generated by entc, DO NOT EDIT.

package entgen

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/harmony-development/legato/server/db/ent/entgen/actiondropdown"
)

// ActionDropdownCreate is the builder for creating a ActionDropdown entity.
type ActionDropdownCreate struct {
	config
	mutation *ActionDropdownMutation
	hooks    []Hook
}

// SetText sets the "text" field.
func (adc *ActionDropdownCreate) SetText(s string) *ActionDropdownCreate {
	adc.mutation.SetText(s)
	return adc
}

// SetOptions sets the "options" field.
func (adc *ActionDropdownCreate) SetOptions(s []string) *ActionDropdownCreate {
	adc.mutation.SetOptions(s)
	return adc
}

// Mutation returns the ActionDropdownMutation object of the builder.
func (adc *ActionDropdownCreate) Mutation() *ActionDropdownMutation {
	return adc.mutation
}

// Save creates the ActionDropdown in the database.
func (adc *ActionDropdownCreate) Save(ctx context.Context) (*ActionDropdown, error) {
	var (
		err  error
		node *ActionDropdown
	)
	if len(adc.hooks) == 0 {
		if err = adc.check(); err != nil {
			return nil, err
		}
		node, err = adc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ActionDropdownMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = adc.check(); err != nil {
				return nil, err
			}
			adc.mutation = mutation
			node, err = adc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(adc.hooks) - 1; i >= 0; i-- {
			mut = adc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, adc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (adc *ActionDropdownCreate) SaveX(ctx context.Context) *ActionDropdown {
	v, err := adc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (adc *ActionDropdownCreate) check() error {
	if _, ok := adc.mutation.Text(); !ok {
		return &ValidationError{Name: "text", err: errors.New("entgen: missing required field \"text\"")}
	}
	if _, ok := adc.mutation.Options(); !ok {
		return &ValidationError{Name: "options", err: errors.New("entgen: missing required field \"options\"")}
	}
	return nil
}

func (adc *ActionDropdownCreate) sqlSave(ctx context.Context) (*ActionDropdown, error) {
	_node, _spec := adc.createSpec()
	if err := sqlgraph.CreateNode(ctx, adc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (adc *ActionDropdownCreate) createSpec() (*ActionDropdown, *sqlgraph.CreateSpec) {
	var (
		_node = &ActionDropdown{config: adc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: actiondropdown.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: actiondropdown.FieldID,
			},
		}
	)
	if value, ok := adc.mutation.Text(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: actiondropdown.FieldText,
		})
		_node.Text = value
	}
	if value, ok := adc.mutation.Options(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: actiondropdown.FieldOptions,
		})
		_node.Options = value
	}
	return _node, _spec
}

// ActionDropdownCreateBulk is the builder for creating many ActionDropdown entities in bulk.
type ActionDropdownCreateBulk struct {
	config
	builders []*ActionDropdownCreate
}

// Save creates the ActionDropdown entities in the database.
func (adcb *ActionDropdownCreateBulk) Save(ctx context.Context) ([]*ActionDropdown, error) {
	specs := make([]*sqlgraph.CreateSpec, len(adcb.builders))
	nodes := make([]*ActionDropdown, len(adcb.builders))
	mutators := make([]Mutator, len(adcb.builders))
	for i := range adcb.builders {
		func(i int, root context.Context) {
			builder := adcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ActionDropdownMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, adcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, adcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, adcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (adcb *ActionDropdownCreateBulk) SaveX(ctx context.Context) []*ActionDropdown {
	v, err := adcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
