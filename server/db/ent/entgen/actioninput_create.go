// Code generated by entc, DO NOT EDIT.

package entgen

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/harmony-development/legato/server/db/ent/entgen/actioninput"
)

// ActionInputCreate is the builder for creating a ActionInput entity.
type ActionInputCreate struct {
	config
	mutation *ActionInputMutation
	hooks    []Hook
}

// SetLabel sets the "label" field.
func (aic *ActionInputCreate) SetLabel(s string) *ActionInputCreate {
	aic.mutation.SetLabel(s)
	return aic
}

// SetWide sets the "wide" field.
func (aic *ActionInputCreate) SetWide(b bool) *ActionInputCreate {
	aic.mutation.SetWide(b)
	return aic
}

// Mutation returns the ActionInputMutation object of the builder.
func (aic *ActionInputCreate) Mutation() *ActionInputMutation {
	return aic.mutation
}

// Save creates the ActionInput in the database.
func (aic *ActionInputCreate) Save(ctx context.Context) (*ActionInput, error) {
	var (
		err  error
		node *ActionInput
	)
	if len(aic.hooks) == 0 {
		if err = aic.check(); err != nil {
			return nil, err
		}
		node, err = aic.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ActionInputMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = aic.check(); err != nil {
				return nil, err
			}
			aic.mutation = mutation
			node, err = aic.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(aic.hooks) - 1; i >= 0; i-- {
			mut = aic.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, aic.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (aic *ActionInputCreate) SaveX(ctx context.Context) *ActionInput {
	v, err := aic.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (aic *ActionInputCreate) check() error {
	if _, ok := aic.mutation.Label(); !ok {
		return &ValidationError{Name: "label", err: errors.New("entgen: missing required field \"label\"")}
	}
	if _, ok := aic.mutation.Wide(); !ok {
		return &ValidationError{Name: "wide", err: errors.New("entgen: missing required field \"wide\"")}
	}
	return nil
}

func (aic *ActionInputCreate) sqlSave(ctx context.Context) (*ActionInput, error) {
	_node, _spec := aic.createSpec()
	if err := sqlgraph.CreateNode(ctx, aic.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (aic *ActionInputCreate) createSpec() (*ActionInput, *sqlgraph.CreateSpec) {
	var (
		_node = &ActionInput{config: aic.config}
		_spec = &sqlgraph.CreateSpec{
			Table: actioninput.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: actioninput.FieldID,
			},
		}
	)
	if value, ok := aic.mutation.Label(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: actioninput.FieldLabel,
		})
		_node.Label = value
	}
	if value, ok := aic.mutation.Wide(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeBool,
			Value:  value,
			Column: actioninput.FieldWide,
		})
		_node.Wide = value
	}
	return _node, _spec
}

// ActionInputCreateBulk is the builder for creating many ActionInput entities in bulk.
type ActionInputCreateBulk struct {
	config
	builders []*ActionInputCreate
}

// Save creates the ActionInput entities in the database.
func (aicb *ActionInputCreateBulk) Save(ctx context.Context) ([]*ActionInput, error) {
	specs := make([]*sqlgraph.CreateSpec, len(aicb.builders))
	nodes := make([]*ActionInput, len(aicb.builders))
	mutators := make([]Mutator, len(aicb.builders))
	for i := range aicb.builders {
		func(i int, root context.Context) {
			builder := aicb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ActionInputMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, aicb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, aicb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, aicb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (aicb *ActionInputCreateBulk) SaveX(ctx context.Context) []*ActionInput {
	v, err := aicb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
